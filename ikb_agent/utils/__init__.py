"""Production integration helpers.

These modules mirror the courseware utilities. They are optional adapters:
the local demo path keeps using JSON storage and lightweight embeddings, while
middleware deployments can import these helpers to connect Milvus, MinIO, and
MongoDB.
"""

